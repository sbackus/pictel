<h1>Add a caption!</h1>

<%= image_tag(@picture_card.data_picture) %>

<%= simple_form_for [@stack, @card] do |f| %>
  <%= f.input :author %>
  <%= f.input :data_text %>

  <%= f.button :submit %>
<% end %>